# Synthetic data boundary

SYNTHETIC DEMONSTRATION DATA — NOT REAL SECURITY TELEMETRY

The fictional organization, users, devices, events, and incidents are generated by `python -m app.db.seed`. No real organization, person, IP infrastructure, policy, or customer data belongs here. Ground-truth scenario labels are exported to ignored `data/runtime/ground_truth.json` for future evaluation only; they are not stored in event columns.
