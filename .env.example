# Local development defaults. Copy to .env only when values need overriding.
# Never commit .env or use real organization data in this demonstration.
APP_ENV=development
LOG_LEVEL=INFO
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3:4b-instruct
QDRANT_URL=http://127.0.0.1:6333
# DATABASE_URL and DEMO_API_TOKEN are required by later phases, not Phase 1.
# DATABASE_URL=postgresql+psycopg://demo:<local-password>@127.0.0.1:5432/security_copilot
# DEMO_API_TOKEN=<generate-a-local-random-token>
